PORT=3000 # Port of the communication server

# ROBLOX_UNIVERSE_ID/ROBLOX_OPEN_CLOUD_API_KEY are no longer read from env - the gateway
# forwards them per-request from the calling game's Game row (x-roblox-universe-id/
# x-roblox-open-cloud-api-key headers), since this service now serves every game.
